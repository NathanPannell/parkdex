{
  "name": "every-park-boundaries",
  "scripts": {
    "android:field-gate": "node scripts/android-field-gate.mjs",
    "test:android-field-gate": "node --test scripts/android-field-gate.test.mjs",
    "test:r2-contract": "python -m pytest scripts/r2_contract_test.py -q",
    "boundaries:build": "node scripts/boundary-build.mjs",
    "boundaries:validate": "node scripts/boundary-validate.mjs",
    "boundaries:test": "node --test scripts/boundary-sources.test.mjs"
  },
  "dependencies": {
    "@neon/config": "^1.1.0",
    "@neon/env": "^1.1.6",
    "railway": "^3.11.0"
  },
  "devDependencies": {
    "@turf/boolean-valid": "^7.4.0",
    "neonctl": "4.14.6"
  }
}
